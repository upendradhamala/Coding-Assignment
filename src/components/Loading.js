import React from "react";

const Loading = () => {
  return (
    <div className="error-msg loading-msg d-flex align-items-center justify-content-center">
      Loading...
    </div>
  );
};

export default Loading;
